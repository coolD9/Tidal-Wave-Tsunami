import React from 'react';
import './CSS/ScheduleDisplay.css';

export default function ScheduleDisplay() {         
    return (
        <div className="ScheduleDisplay-container">
            <h2 className="title">Schedule Display</h2>
            <p>This is where the optimized schedule will be displayed.</p>
        </div>
    );
}